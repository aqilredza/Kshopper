// This is a conceptual SQL test - you would need to run this in your Supabase SQL editor\nconsole.log(`\n-- Run this in your Supabase SQL editor to check for triggers and functions:\n\n-- Check for triggers on custom_requests table\nSELECT \n  tgname as trigger_name,\n  tgrelid::regclass as table_name,\n  tgfoid::regproc as function_name\nFROM pg_trigger\nWHERE tgrelid = 'custom_requests'::regclass;\n\n-- Check for any functions that might affect custom_requests\nSELECT \n  proname as function_name,\n  prosrc as function_source\nFROM pg_proc\nWHERE prosrc ILIKE '%custom_requests%';\n\n-- Check the structure of custom_requests table\n\\\\d custom_requests\n\n-- Check if there are any policies that might be interfering\nSELECT \n  polname as policy_name,\n  polrelid::regclass as table_name\nFROM pg_policy\nWHERE polrelid::regclass::text = 'custom_requests';\n\n-- Try a direct update to see if it works\nUPDATE custom_requests \nSET status = 'testing_update' \nWHERE id = (SELECT id FROM custom_requests LIMIT 1);\n\n-- Check if the update was applied\nSELECT id, status FROM custom_requests \nWHERE status = 'testing_update' \nLIMIT 1;\n`);